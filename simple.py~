#!/usr/bin/env python
#-*- coding: utf-8 -*-

# Usage: ./$0 ‹file utf-8 encoded› [--options]

import sys
raw = open(sys.argv[1]).read().decode('utf-8')

import re
lateral = 'jilk'; middle = '-|-'
regex = re.compile(lateral +" \s*(.+)\s* \-\|\- \s*(.+)\s* "+ lateral)

match = regex.findall(raw)
if match == []:

